\usepackage{booktabs}
\usepackage{titlesec}
\usepackage[german]{babel}

% Redefine chapter format: number + title only
\titleformat{\chapter}[hang]
  {\normalfont\huge\bfseries} % font style
  {\thechapter}               % just the number
  {1em}                       % spacing between number and title
  {}                          % before-code (empty)
